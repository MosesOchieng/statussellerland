services:
  - type: web
    name: statusseller-site
    runtime: static
    buildCommand: pnpm install --frozen-lockfile && BASE_PATH=/ PORT=10000 pnpm --filter @workspace/statusseller-site run build
    staticPublishPath: artifacts/statusseller-site/dist/public
    previews:
      generation: automatic